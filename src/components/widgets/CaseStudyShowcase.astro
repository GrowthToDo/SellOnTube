---
export interface Props {
  title?: string;
  tagline?: string;
}

const {
  title = 'See the proof',
  tagline = 'Case Study',
} = Astro.props;
---

<section class="relative py-16 md:py-20 bg-slate-900 overflow-hidden">
  <!-- Background pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg width=\"60\" height=\"60\" viewBox=\"0 0 60 60\" xmlns=\"http://www.w3.org/2000/svg\"%3E%3Cg fill=\"none\" fill-rule=\"evenodd\"%3E%3Cg fill=\"%23ffffff\" fill-opacity=\"0.4\"%3E%3Cpath d=\"M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z\"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
  </div>

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6">
    <!-- Tagline -->
    <div class="text-center mb-8">
      <span class="inline-block px-3 py-1 text-xs font-semibold tracking-wider text-red-400 uppercase bg-red-500/10 rounded-full">
        {tagline}
      </span>
    </div>

    <!-- Main content -->
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">

      <!-- Left: Stats & headline -->
      <div class="text-center lg:text-left">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6 leading-tight">
          12-Month Experiment:<br/>
          <span class="text-red-500">YouTube vs Blog</span>
        </h2>

        <!-- Big stat -->
        <div class="mb-8">
          <div class="inline-flex items-baseline gap-2">
            <span class="text-7xl md:text-8xl font-black text-white">3.25</span>
            <span class="text-4xl md:text-5xl font-bold text-red-500">x</span>
          </div>
          <p class="text-xl text-slate-300 mt-2">more conversions from YouTube</p>
        </div>

        <!-- Mini stats -->
        <div class="grid grid-cols-2 gap-4 mb-8 max-w-sm mx-auto lg:mx-0">
          <div class="bg-white/5 rounded-xl p-4 text-center">
            <div class="text-2xl font-bold text-white">1,257</div>
            <div class="text-sm text-slate-400">YouTube conversions</div>
          </div>
          <div class="bg-white/5 rounded-xl p-4 text-center">
            <div class="text-2xl font-bold text-white">411</div>
            <div class="text-sm text-slate-400">Blog conversions</div>
          </div>
        </div>

        <a
          href="/blog/youtube-vs-blog-shopify-app-case-study"
          class="inline-flex items-center gap-2 px-6 py-3 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg transition-colors"
        >
          Read the full case study
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>

      <!-- Right: Visual chart representation -->
      <div class="relative">
        <div class="bg-white/5 backdrop-blur rounded-2xl p-6 md:p-8">
          <h3 class="text-lg font-semibold text-white mb-6">Monthly Conversions: The Gap Widened</h3>

          <!-- Simplified visual bar chart -->
          <div class="space-y-4">
            {[
              { month: 'Jun', yt: 78, blog: 31 },
              { month: 'Sep', yt: 98, blog: 34 },
              { month: 'Dec', yt: 112, blog: 34 },
              { month: 'Mar', yt: 132, blog: 34 },
              { month: 'May', yt: 142, blog: 29 },
            ].map((data) => (
              <div class="flex items-center gap-3">
                <span class="w-10 text-sm text-slate-400">{data.month}</span>
                <div class="flex-1 flex gap-1 items-center">
                  <div
                    class="h-6 bg-red-500 rounded-sm"
                    style={`width: ${(data.yt / 150) * 100}%`}
                  />
                  <span class="text-xs text-red-400 font-medium">{data.yt}</span>
                </div>
              </div>
              <div class="flex items-center gap-3">
                <span class="w-10"></span>
                <div class="flex-1 flex gap-1 items-center">
                  <div
                    class="h-6 bg-blue-500 rounded-sm"
                    style={`width: ${(data.blog / 150) * 100}%`}
                  />
                  <span class="text-xs text-blue-400 font-medium">{data.blog}</span>
                </div>
              </div>
            ))}
          </div>

          <!-- Legend -->
          <div class="flex gap-6 mt-6 pt-4 border-t border-white/10">
            <div class="flex items-center gap-2">
              <div class="w-3 h-3 bg-red-500 rounded-sm"></div>
              <span class="text-sm text-slate-300">YouTube</span>
            </div>
            <div class="flex items-center gap-2">
              <div class="w-3 h-3 bg-blue-500 rounded-sm"></div>
              <span class="text-sm text-slate-300">Blog</span>
            </div>
          </div>
        </div>

        <!-- Decorative elements -->
        <div class="absolute -top-4 -right-4 w-24 h-24 bg-red-500/20 rounded-full blur-2xl"></div>
        <div class="absolute -bottom-4 -left-4 w-32 h-32 bg-blue-500/10 rounded-full blur-2xl"></div>
      </div>
    </div>

    <!-- Bottom quote -->
    <div class="mt-12 text-center">
      <p class="text-slate-400 italic max-w-2xl mx-auto">
        "By May 2025, YouTube was converting at nearly <span class="text-white font-semibold">5x the rate</span> of blog content."
      </p>
    </div>
  </div>
</section>
